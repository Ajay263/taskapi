[pytest]
# Run tests from the repo root so Python can find 'app.main' as a package import
# (import path: app.main — 'app' is the folder, 'main' is the module)
asyncio_mode = auto

# Default test paths
testpaths = app/tests

# Show extra detail on failures
addopts = -v --tb=short